<Window x:Class="UrbanChaosMapEditor.Views.Dialogs.Buildings.CableFacetPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cable Preview"
        Height="400"
        Width="700"
        Background="#222222"
        Foreground="White"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Name="HeaderTextBlock"
                   Grid.Row="0"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,4" />

        <!-- Details / parameters -->
        <TextBlock x:Name="DetailsTextBlock"
                   Grid.Row="1"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"
                   Opacity="0.8" />

        <!-- Drawing surface -->
        <Border Grid.Row="2"
                BorderBrush="#444"
                BorderThickness="1"
                CornerRadius="4"
                Background="#111111">
            <Canvas x:Name="PreviewCanvas" />
        </Border>
    </Grid>
</Window>
